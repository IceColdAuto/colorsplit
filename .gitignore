# dependencies
node_modules

# build output
dist
dist-ssr

# local env files (set these in Vercel dashboard instead — never commit)
.env
.env.local
.env.*.local

# logs / misc
*.log
.DS_Store
.vercel
